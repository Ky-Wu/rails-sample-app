<% provide(:title, "Home") %>
<% if logged_in? %>
  <div class="row">
    <%= render 'shared/user_aside' %>
    <div class="col-md-8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <%= render 'home_page' %>
<% end %>
